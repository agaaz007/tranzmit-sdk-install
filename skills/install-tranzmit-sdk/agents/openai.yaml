display_name: Install Tranzmit With Claude
short_description: Patch a client app to add Tranzmit widget or replay with a coding agent.
default_prompt: Install Tranzmit in this codebase using the app's existing auth, layout, and framework patterns.
